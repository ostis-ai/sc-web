{
  "name": "sc-web",
  "version": "0.9.0",
  "module": "commonjs",
  "dependencies": {
    "grunt": "^1.0.1",
    "ts-sc-client": "^0.5.0"
  },

  "scripts": {
	  "build": "grunt build",
	  "serve": "grunt",
	  "test": "grunt test"
  },

  "devDependencies": {
    "grunt-contrib-concat": "^1.0.1",
    "grunt-contrib-copy": "^1.0.0",
    "grunt-contrib-watch": "^1.0.0",
    "grunt-cli": "^1.4.3"
  }
}
