{
  "name": "sc-web",
  "version": "1.0.0",
  "dependencies": {
    "grunt": "^1.0.1",
    "ts-sc-client": "^0.2.0"
  },

  "scripts": {
	  "build": "grunt build",
	  "serve": "grunt",
	  "test": "grunt test"
  },

  "devDependencies": {
    "grunt-contrib-concat": "^1.0.1",
    "grunt-contrib-copy": "^1.0.0",
    "grunt-contrib-watch": "^1.0.0",
    "grunt-cli": "^1.4.3"
  }
}
